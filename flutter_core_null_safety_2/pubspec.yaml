name: flutter_core_null_safety_2
description: A new Flutter project.
version: 0.0.1
homepage:


environment:
  sdk: ">=2.16.0 <3.0.0"
  flutter: ">=1.20.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_bloc: ^8.0.1
  dio: ^4.0.6
  mockito: ^5.1.0
  equatable: ^2.0.3
  get_it: ^7.2.0
  connectivity_plus: ^2.3.0
  intl: ^0.17.0


dev_dependencies:
  flutter_test:
    sdk: flutter

# The following section is specific to Flutter.
flutter:
  # This section identifies this Flutter project as a plugin project.
  # The 'pluginClass' and Android 'package' identifiers should not ordinarily
  # be modified. They are used by the tooling to maintain consistency when
  # adding or updating assets for this project.
  plugin:
    platforms:
      android:
        package: com.example.flutter_core_null_safety_2
        pluginClass: FlutterCoreNullSafety_2Plugin
      ios:
        pluginClass: FlutterCoreNullSafety_2Plugin

